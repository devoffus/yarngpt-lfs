version: '3.8'

services:
  yarngpt-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEVICE=${DEVICE:-cpu}
    volumes:
      - model-cache:/app/models
      - data-cache:/app/data

volumes:
  model-cache:
  data-cache: